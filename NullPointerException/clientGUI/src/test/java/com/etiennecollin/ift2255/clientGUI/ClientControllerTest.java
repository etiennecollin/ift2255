package com.etiennecollin.ift2255.clientGUI;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * The `ClientControllerTest` class contains test cases for the client controller methods in the `ClientController`
 * class.
 * It uses JUnit 5 to create and run the tests.
 */
public class ClientControllerTest {
    /**
     * Test case for the `X` method in the `X` class.
     * It verifies that the `X` method correctly XXX.
     */
    @Test
    public void testMethod() {
        // assertEquals(expected, actual);
    }
}
